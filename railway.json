{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd backend && pip install -r requirements.txt"
    },
    "deploy": {
        "startCommand": "cd backend && python -c \"import os; port=os.environ.get('PORT', '8000'); import subprocess; subprocess.run(['uvicorn', 'main:app', '--host', '0.0.0.0', '--port', port])\"",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}