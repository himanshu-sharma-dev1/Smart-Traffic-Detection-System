# 🏗️ Smart Traffic Detection System - Architecture

## 📋 Table of Contents
- [System Overview](#system-overview)
- [Tech Stack](#tech-stack)
- [Dual-Model Detection Architecture](#dual-model-detection-architecture)
- [Data Flow](#data-flow)
- [Component Architecture](#component-architecture)
- [API Architecture](#api-architecture)
- [Custom YOLOv8 Model](#custom-yolov8-model)
- [Detection Pipeline](#detection-pipeline)
- [Advanced Features](#advanced-features)

---

## 🌐 System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT (Browser)                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        React 19 Application                          │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │  Home   │ │ Detect  │ │  Live   │ │Dashboard│ │   Settings  │   │   │
│  │  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘ └──────┬──────┘   │   │
│  │       │           │           │           │              │           │   │
│  │  ┌────┴───────────┴───────────┴───────────┴──────────────┴────┐     │   │
│  │  │                  Detection Engine Layer                     │     │   │
│  │  │  ┌────────────────────┐   ┌─────────────────────────────┐  │     │   │
│  │  │  │     COCO-SSD       │   │   Custom YOLOv8 (85 cls)    │  │     │   │
│  │  │  │  (80 classes)      │   │   Indian Traffic Signs       │  │     │   │
│  │  │  │  - Vehicles        │   │   - STOP, Speed Limits       │  │     │   │
│  │  │  │  - Pedestrians     │   │   - Warning Signs            │  │     │   │
│  │  │  │  - Traffic Lights  │   │   - Regulatory Signs         │  │     │   │
│  │  │  └─────────┬──────────┘   └──────────────┬──────────────┘  │     │   │
│  │  │            │                              │                  │     │   │
│  │  │  ┌─────────┴──────────────────────────────┴──────────────┐  │     │   │
│  │  │  │              Detection Merger (NMS)                    │  │     │   │
│  │  │  │   - Cross-model Non-Maximum Suppression               │  │     │   │
│  │  │  │   - Duplicate removal (stop sign from both models)    │  │     │   │
│  │  │  │   - Confidence-based filtering                        │  │     │   │
│  │  │  └────────────────────────────────────────────────────────┘  │     │   │
│  │  │                            │                                  │     │   │
│  │  │  ┌─────────────────────────┴────────────────────────────┐    │     │   │
│  │  │  │                 Post-Processing                       │    │     │   │
│  │  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐│    │     │   │
│  │  │  │  │  SORT    │ │  Speed   │ │ Counting │ │ License  ││    │     │   │
│  │  │  │  │ Tracker  │ │Estimator │ │  Zones   │ │ Plate OCR││    │     │   │
│  │  │  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘│    │     │   │
│  │  │  └──────────────────────────────────────────────────────┘    │     │   │
│  │  └──────────────────────────────────────────────────────────────┘     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ REST API (Axios)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             SERVER (FastAPI)                                 │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │                         API Routes                                  │    │
│  │   /api/auth/*          /api/detections/*         /detect           │    │
│  │   - register           - GET (list)              - POST (upload)   │    │
│  │   - login              - POST (create)           - Gemini Vision   │    │
│  │   - forgot-password    - DELETE (remove)                           │    │
│  │   - reset-password     - stats                                     │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                      │                                       │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │                      Services Layer                                 │    │
│  │  ┌────────────┐ ┌────────────────┐ ┌─────────────┐ ┌────────────┐ │    │
│  │  │  JWT Auth  │ │ Password Hash  │ │ Email SMTP  │ │ Rate Limit │ │    │
│  │  │ (jose)     │ │ (bcrypt)       │ │ (aiosmtplib)│ │ (slowapi)  │ │    │
│  │  └────────────┘ └────────────────┘ └─────────────┘ └────────────┘ │    │
│  └────────────────────────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                    ┌─────────────────────────────────┐
                    │         MongoDB Atlas           │
                    │   ┌─────────┐   ┌───────────┐  │
                    │   │  Users  │   │ Detections│  │
                    │   └─────────┘   └───────────┘  │
                    └─────────────────────────────────┘
```

---

## 🛠️ Tech Stack

| Layer | Technology | Purpose |
|-------|------------|---------|
| **Frontend** | React 19 | UI Framework |
| | TensorFlow.js | Browser ML inference (COCO-SSD, YOLOv8) |
| | React Bootstrap | Component library |
| | Framer Motion | Animations |
| | Recharts | Analytics charts |
| | ~~Tesseract.js~~ | License plate OCR (hidden - server-side) |
| **Backend** | FastAPI | Async Python web framework |
| | Motor | Async MongoDB driver |
| | Pydantic | Data validation |
| | python-jose | JWT tokens |
| | google-generativeai | Gemini Vision API |
| **Database** | MongoDB Atlas | Cloud NoSQL database |
| **Infrastructure** | PWA | Installable, offline-capable |
| | Service Workers | Caching, background sync |

---

## 🤖 Dual-Model Detection Architecture

```
                              ┌─────────────────────┐
                              │    Video Frame      │
                              │   (640x480 px)      │
                              └──────────┬──────────┘
                                         │
              ┌──────────────────────────┼──────────────────────────┐
              │                          │                          │
              ▼                          │                          ▼
┌─────────────────────────┐              │            ┌─────────────────────────┐
│       COCO-SSD          │              │            │   Custom YOLOv8n        │
│  (TensorFlow.js)        │              │            │  (TensorFlow.js)        │
├─────────────────────────┤              │            ├─────────────────────────┤
│ Input: 300×300          │              │            │ Input: 512×512          │
│ Classes: 80 (COCO)      │              │            │ Classes: 85 (signs)     │
│ Speed: ~30ms/frame      │              │            │ Speed: ~50ms/frame      │
│ Output: [x,y,w,h,cls,c] │              │            │ Output: [x,y,w,h,cls,c] │
└──────────┬──────────────┘              │            └──────────┬──────────────┘
           │                             │                       │
           │                             │                       │
           │    ┌────────────────────────┴────────────────────┐  │
           │    │            Frame Interlacing                 │  │
           │    │  COCO-SSD: Every frame                       │  │
           │    │  YOLOv8: Every 2nd frame (position interp)   │  │
           └───►│                                              │◄─┘
                └────────────────────────┬────────────────────┘
                                         │
                                         ▼
                ┌────────────────────────────────────────────┐
                │           Detection Merger                  │
                │  ┌──────────────────────────────────────┐  │
                │  │  Step 1: Normalize formats            │  │
                │  │  COCO: {class, score, bbox}          │  │
                │  │  YOLO: {class, score, bbox, source}  │  │
                │  └──────────────────────────────────────┘  │
                │  ┌──────────────────────────────────────┐  │
                │  │  Step 2: Sort by confidence (desc)   │  │
                │  └──────────────────────────────────────┘  │
                │  ┌──────────────────────────────────────┐  │
                │  │  Step 3: Cross-Model NMS (IoU=0.5)   │  │
                │  │  - Remove duplicate "stop sign"      │  │
                │  │  - Keep highest confidence           │  │
                │  └──────────────────────────────────────┘  │
                └────────────────────────┬───────────────────┘
                                         │
                                         ▼
                            ┌──────────────────────┐
                            │  Merged Detections   │
                            │  (Deduplicated)      │
                            └──────────────────────┘
```

### Why Dual-Model Approach?

| Decision | Reasoning |
|----------|-----------|
| **COCO-SSD for general objects** | Lightweight (~4MB), fast inference, good for vehicles/pedestrians |
| **Custom YOLOv8 for signs** | Fine-tuned on Indian signs, 91.5% mAP50, domain-specific |
| **Frame interlacing** | YOLOv8 runs every 2nd frame to maintain 16+ FPS |
| **Position interpolation** | Smooth bounding boxes between frames |
| **Cross-model NMS** | Prevents duplicate boxes when both models detect same object |

---

## 🚦 Custom YOLOv8 Model

### Model Specifications

| Spec | Value |
|------|-------|
| Base Architecture | YOLOv8n (Nano) |
| Input Size | 512 × 512 |
| Classes | 85 Indian traffic signs |
| Model Size | ~12MB (TensorFlow.js) |
| mAP50 | 91.5% |
| mAP50-95 | 85.1% |
| Recall | 92.7% |
| Precision | 82.2% |

### Training Pipeline

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        Training Pipeline (Google Colab)                   │
└──────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Step 1: Dataset Preparation                                              │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  - 2000+ Indian traffic sign images                                 │  │
│  │  - YOLO format annotations (class_id x_center y_center w h)        │  │
│  │  - 85 classes (STOP, SPEED_LIMIT_*, NO_PARKING, etc.)             │  │
│  │  - Train/Val split: 80/20                                          │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                     │                                      │
│                                     ▼                                      │
│  Step 2: Training (Ultralytics YOLOv8)                                    │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  model = YOLO('yolov8n.pt')  # Pretrained on COCO                  │  │
│  │  model.train(data='traffic_signs.yaml', epochs=100, imgsz=512)     │  │
│  │  # Transfer learning: freeze backbone, train head                  │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                     │                                      │
│                                     ▼                                      │
│  Step 3: Export to TensorFlow.js                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  model.export(format='tfjs')                                        │  │
│  │  # Generates: model.json, group1-shard1of4.bin, ...                │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Deployment: frontend/public/models/traffic_signs/                        │
│  ├── model.json                 # Model architecture                      │
│  ├── group1-shard1of4.bin       # Weights (split into shards)            │
│  ├── group1-shard2of4.bin                                                 │
│  ├── group1-shard3of4.bin                                                 │
│  ├── group1-shard4of4.bin                                                 │
│  └── class_names.json           # 85 class labels                         │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Detection Pipeline (Live Mode)

```
┌─────────────────────────────────────────────────────────────────────────┐
│   LiveDetection.js - Real-time Processing Pipeline                       │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────┐   ┌──────────┐   ┌──────────────────────────────────────────┐
│  Webcam  │──▶│  Canvas  │──▶│ requestAnimationFrame Loop (~30fps)      │
│  Stream  │   │  640×480 │   │                                          │
└──────────┘   └──────────┘   │  ┌────────────────────────────────────┐  │
                              │  │ 1. Capture frame from video         │  │
                              │  └────────────────────────────────────┘  │
                              │                   │                       │
                              │                   ▼                       │
                              │  ┌────────────────────────────────────┐  │
                              │  │ 2. Run COCO-SSD (every frame)      │  │
                              │  │    + YOLOv8 (every 2nd frame)      │  │
                              │  └────────────────────────────────────┘  │
                              │                   │                       │
                              │                   ▼                       │
                              │  ┌────────────────────────────────────┐  │
                              │  │ 3. Merge detections (NMS)          │  │
                              │  └────────────────────────────────────┘  │
                              │                   │                       │
                              │                   ▼                       │
                              │  ┌────────────────────────────────────┐  │
                              │  │ 4. SORT Tracker (assign IDs)       │  │
                              │  │    - IoU matching across frames    │  │
                              │  │    - Persistent object IDs         │  │
                              │  └────────────────────────────────────┘  │
                              │                   │                       │
                              │          ┌───────┴────────┐              │
                              │          ▼                ▼              │
                              │  ┌──────────────┐ ┌──────────────────┐  │
                              │  │ Speed Est.   │ │ Counting Zones   │  │
                              │  │ (optional)   │ │ (optional)       │  │
                              │  └──────────────┘ └──────────────────┘  │
                              │                   │                       │
                              │                   ▼                       │
                              │  ┌────────────────────────────────────┐  │
                              │  │ 5. Draw on canvas                  │  │
                              │  │    - Bounding boxes                │  │
                              │  │    - Labels + confidence           │  │
                              │  │    - Movement trails               │  │
                              │  │    - Speed indicators              │  │
                              │  └────────────────────────────────────┘  │
                              └──────────────────────────────────────────┘
```

---

## 🔧 Advanced Feature Modules

### 1. SpeedEstimator

```
┌─────────────────────────────────────────────────────────────┐
│  SpeedEstimator - Physics-based speed calculation            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Frame N-1          Frame N            Calculation           │
│  ┌──────┐           ┌──────┐                                │
│  │  🚗  │ ────────▶ │  🚗  │    displacement = √(Δx²+Δy²)   │
│  └──────┘           └──────┘                                │
│  (x1,y1)           (x2,y2)    speed = displacement / Δtime  │
│                                                              │
│  Calibration: pixels_per_meter (user adjustable)            │
│  Smoothing: 5-frame moving average                          │
│  Output: km/h, m/s, or mph                                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 2. CountingZone

```
┌─────────────────────────────────────────────────────────────┐
│  CountingZone - Line-crossing detection                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│    ┌────────────────────────────────────────┐               │
│    │                                        │               │
│    │     🚗 ────▶ ════════════════          │  Count: IN    │
│    │              Counting Line              │               │
│    │     ◀──── 🚗 ════════════════          │  Count: OUT   │
│    │                                        │               │
│    └────────────────────────────────────────┘               │
│                                                              │
│  Algorithm: Cross-product to determine side of line          │
│  side = (x2-x1)*(y-y1) - (y2-y1)*(x-x1)                     │
│  Crossing: sign change between frames                        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 3. LicensePlateDetector

```
┌─────────────────────────────────────────────────────────────┐
│  LicensePlateDetector - OCR Pipeline                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────┐                                         │
│  │   Vehicle Box  │   1. Detect vehicle (COCO-SSD)          │
│  │   ┌──────────┐ │                                         │
│  │   │ 🚗       │ │   2. Crop lower 25% (plate region)      │
│  │   │          │ │                                         │
│  │   │ [AB-1234]│ │   3. Preprocess:                        │
│  │   └──────────┘ │      - Grayscale                        │
│  └────────────────┘      - Contrast enhancement             │
│                          - Binary threshold                  │
│          │                                                   │
│          ▼               4. Scale up 2x for better OCR       │
│  ┌────────────────┐                                         │
│  │  EasyOCR       │      5. OCR with alphanumeric whitelist (server)│
│  │  "MH 12 AB 1234" │                                       │
│  └────────────────┘      6. Cache result (3s cooldown)      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔌 API Architecture

### Endpoint Overview

```
/api/auth/                           /api/detections/           /detect
├── POST /register                   ├── GET /                  └── POST
│   └── Create user + send email     │   └── List (paginated)       └── Gemini Vision API
├── POST /login                      ├── POST /                     
│   └── Return JWT token             │   └── Save detection             
├── POST /forgot-password            ├── DELETE /{id}               
│   └── Send reset email             │   └── Remove detection          
├── POST /reset-password             └── GET /stats                 
│   └── Update password                  └── User statistics         
├── GET /me                                                        
│   └── Current user profile                                       
└── DELETE /me                                                     
    └── Permanent deletion                                         
```

---

## 📁 Directory Structure

```
Smart Traffic Detection System/
├── 📂 backend/
│   ├── main.py                    # FastAPI + Gemini Vision
│   ├── config/
│   │   ├── database.py            # MongoDB connection
│   │   └── settings.py            # Environment config
│   ├── models/schemas.py          # Pydantic models
│   ├── routes/
│   │   ├── auth.py                # Authentication endpoints
│   │   └── detection.py           # Detection CRUD
│   ├── utils/
│   │   ├── email.py               # SMTP email service
│   │   ├── jwt.py                 # Token handling
│   │   └── exceptions.py          # Custom errors
│   └── requirements.txt
│
├── 📂 frontend/
│   ├── public/
│   │   ├── models/traffic_signs/  # YOLOv8 TF.js model
│   │   ├── manifest.json          # PWA manifest
│   │   └── service-worker.js      # Offline support
│   └── src/
│       ├── components/            # Reusable UI components
│       ├── context/AuthContext.js # Auth state management
│       ├── hooks/
│       │   ├── usePWAInstall.js   # PWA installation
│       │   └── useShortcuts.js    # Keyboard shortcuts
│       ├── utils/
│       │   ├── TrafficSignDetector.js   # YOLOv8 wrapper
│       │   ├── DetectionMerger.js       # Cross-model NMS
│       │   ├── SimpleTracker.js         # SORT algorithm
│       │   ├── SpeedEstimator.js        # Speed calculation
│       │   ├── CountingZone.js          # Line-crossing
│       │   ├── LicensePlateDetector.js  # OCR
│       │   └── exportPdf.js             # PDF generation
│       ├── App.js                 # Main routing
│       ├── LiveDetection.js       # Real-time detection
│       ├── Detection.js           # Image upload
│       ├── Dashboard.js           # Analytics
│       ├── Settings.js            # User preferences
│       ├── Profile.js             # User profile
│       └── History.js             # Detection history
│
└── 📂 notebooks/
    └── train_traffic_signs_yolov8.ipynb  # Colab training notebook
```

---

## 🎯 Key Design Decisions

| Decision | Why |
|----------|-----|
| **TensorFlow.js for browser inference** | No server round-trip, works offline, lower latency |
| **Dual-model with frame interlacing** | Balance accuracy (YOLOv8) with speed (COCO-SSD) |
| **MongoDB for storage** | Flexible schema, easy cloud deployment (Atlas) |
| **FastAPI async** | Non-blocking I/O, high performance |
| **Gemini Vision for static detection** | Superior accuracy for uploaded images |
| **LocalStorage for profile image** | No backend changes needed, simple implementation |
| **Service Workers for PWA** | Offline capability, installable app |
